//#include<stdio.h>
//#include <queue>
//#include <stack>
//#include <iostream>
//#include <vector>
//
//using namespace std;
//
//int main(void) {
////    queue<int> q; // 创建队列
////    q.push(1);    // 入队
////    q.push(2);
////    int front = q.front();     // 获取队首元素 还没有删除
////    cout << front << endl;
////    q.pop();                   // 删除了队首元素
////    cout <<  q.size() << endl;
//
////    stack<int > s; // 创建栈
////    s.size();      // 大小
////    s.push(1);
////    s.push(2);
////    while (s.size() != 0) {
////        int top = s.top(); // 获取栈顶 但是还没有删除
////        cout << top << endl;
////        s.pop(); // 删除
////    }
//
//    vector<int> arr;
//    for (int i = 0; i < 5; ++i) {
//        arr.push_back(i);
//    }
//    for (int i = 0; i < arr.size(); ++i) {
//        cout << arr[i] << endl;
//
//    }
//    for (int i = 0; i < 5; ++i) {
//        arr.pop_back();
//    }
//    stack<vector<int>> s;
//    s.push(arr);
//
//    cout << "szie :"  << arr.size() << endl;
//    return 0;
//}